
{
  "name": "portfolio-app",
  "version": "1.0.0",
  "description": "Personal Portfolio with DevOps Pipeline",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest --passWithNoTests",
    "docker:build": "docker build -t portfolio-app .",
    "docker:run": "docker run -p 3000:3000 -d portfolio-app",
    "docker:stop": "docker stop $(docker ps -q --filter ancestor=portfolio-app)"
  },
  "keywords": ["portfolio", "devops", "docker", "ci-cd"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "4.18.2",
    "ejs": "3.1.9",
    "dotenv": "16.0.3"
  },
  "devDependencies": {
    "nodemon": "3.0.1",
    "jest": "29.7.0",
    "supertest": "6.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
